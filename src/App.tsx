// Phase 0 scaffold placeholder. Real screens (landing → category → game → win)
// arrive in Phase 2. This blank, Tailwind-styled page exists only to prove the
// toolchain (Vite + React + TS + Tailwind) is wired up correctly.
function App() {
  return (
    <div className="min-h-screen flex flex-col items-center justify-center bg-gradient-to-br from-slate-900 to-slate-700 text-white">
      <h1 className="text-4xl font-bold tracking-tight">🎯 Meeting Bingo</h1>
      <p className="mt-3 text-slate-300">Scaffold ready — build starts in Phase 1.</p>
    </div>
  );
}

export default App;
